/* eslint-disable no-unused-vars */
export enum Action {
    hide = 'Hide',
    show = 'Show',
    newLoop = 'New Loop',
    waitingForPlayers = 'Waiting for players',
    join = 'Join',
    create = 'Create',
    register = 'Register',
    ready = 'ready',
    nextTurn = 'nextTurn',
    hit = 'hit',
    victory = 'victory'
}
